import React from 'react';
import UseTitle from '../../../hooks/UseTitle';

const Home = () => {
    UseTitle('Home')
    return (
        <div>
            <h2>Home components</h2>
        </div>
    );
};

export default Home;