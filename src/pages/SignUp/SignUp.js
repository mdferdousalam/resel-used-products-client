import React from 'react';
import UseTitle from '../../hooks/UseTitle';

const SignUp = () => {
    UseTitle('SignUp')
    return (
        <div>
            SignUp
        </div>
    );
};

export default SignUp;