import React from 'react';
import UseTitle from '../../hooks/UseTitle';

const Login = () => {
    UseTitle('Login')
    return (
        <div>
            Login
        </div>
    );
};

export default Login;